# Invalid: decision named 'options' clashes with a reserved category
# name used in tree-path references (e.g. decisions.<id>.options.<id>).
id: bad_reserved_decision
name: Reserved decision name test
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
decisions:
  options:
    id: options
    label: Reserved name
    rationale: should fail validation
    default: a
    options:
      a:
        label: A
