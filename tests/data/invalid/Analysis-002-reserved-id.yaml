# Invalid: sub-analysis named 'decisions' shadows the reserved category.
# Tree-path refs like `decisions.foo` would be ambiguous against this sub.
id: bad_reserved_analysis
name: Reserved-name shadowing test
inputs:
  - id: data
    type: data
outputs:
  - id: result
    type: metric
analyses:
  decisions:
    id: decisions
    inputs:
      - id: x
        type: data
    outputs:
      - id: y
        type: metric
