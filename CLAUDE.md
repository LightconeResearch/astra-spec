# ASTRA Spec - Development Notes

## Schema and Documentation

The ASTRA schema is defined in LinkML files under `src/astra/schema/`. When modifying any schema file (`analysis.yaml`, `insight.yaml`, `universe.yaml`), you **must** also update the corresponding documentation:

- **`docs/specification/draft/format.md`** — The human-readable specification. Update field tables, examples, and descriptions to reflect schema changes.
- **`docs/index.md`** — Landing page; update the quick "At a glance" example if the change affects it.
- **`docs/cli.md`** — CLI reference; update if the schema change adds a new validation rule or command surface.
- **`README.md`** — If the change affects the quick example or design principles, update the README too.

Run `just gen-python` to regenerate Python datamodels and `just gen-doc` to regenerate the schema reference docs after any schema change.

## Versioned specification layout

The specification is versioned MCP-style: every release ships its own immutable folder under `docs/specification/`. Active editing happens in `draft/`; all other folders are frozen snapshots.

```
docs/specification/
├── versions.json            # registry consumed by the version-selector widget
├── draft/                   # in-development; edit here
│   ├── format.md            # hand-authored prose
│   ├── elements/            # auto-generated by `just gen-doc` (gitignored)
│   └── schema/              # auto-generated artifacts (yaml, json, jsonld)
└── 0.0.9/, 0.0.10/, ...     # frozen snapshots — DO NOT edit
```

Rules:

- Edit `docs/specification/draft/format.md` only. Frozen `<version>/` folders are immutable historical records.
- `just gen-doc` regenerates only `draft/elements/` and `draft/schema/`. The frozen versions are untouched.
- `just release X.Y.Z` is the only thing that creates a new frozen version: it bumps schema YAMLs, regenerates draft, snapshots `draft/` → `X.Y.Z/`, updates `versions.json`, rewrites the nav and inbound links from `specification/<previous>/` to `specification/X.Y.Z/`, then commits and tags.
- Inbound links from unversioned docs (`index.md`, `getting-started.md`, `community.md`) point at the current latest version (or `draft` before any release). The release recipe rewrites them automatically.
- The version dropdown is a client-side widget (`docs/assets/js/version-selector.js`) reading `versions.json`. It is theme-independent — no Zensical/MkDocs plugin required.

## Key Commands

```bash
just test        # Run all tests
just gen-python  # Regenerate Python datamodels from schema
just gen-doc     # Regenerate schema reference documentation
just lint        # Lint the schema
just docs-serve  # Build and preview docs locally with live reload
```

## Repository Layout

- `src/astra/schema/` — LinkML schema source files (edit these)
- `src/astra/datamodel/` — Generated Python datamodel (do not edit directly)
- `docs/` — Documentation source (rendered with [Zensical](https://zensical.org/); config in `zensical.toml`)
- `docs/index.md` — Landing page (clean, MCP-style with card grid nav)
- `docs/getting-started.md` — Install + scaffold + validate tutorial
- `docs/specification.md` — The ASTRA format specification
- `docs/cli.md` — `astra` CLI reference (covers commands shipped by `astra-tools`)
- `docs/elements/` — Auto-generated schema reference (regenerated by `just gen-doc`)
- `examples/` — Example ASTRA projects (iris, iris_pipeline)
- `tests/data/` — Test fixtures (valid, invalid, problem)
